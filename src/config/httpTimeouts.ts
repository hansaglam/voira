/** Speech analysis multipart upload (includes server processing time). */
export const ANALYSIS_UPLOAD_TIMEOUT_MS = 90_000;

/** Account deletion POST timeout. */
export const ACCOUNT_DELETE_TIMEOUT_MS = 30_000;

/** Remote lesson audio registry fetch timeout. */
export const AUDIO_REGISTRY_FETCH_TIMEOUT_MS = 15_000;

/** Cloud progress / sync network timeouts (ms). */
export const PROGRESS_SYNC_TIMEOUT_MS = 15_000;
